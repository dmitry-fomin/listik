шаг 01: старт — спека docs/specs/steps/step-01-documents-and-context.md (продукт docs/specs/listik-product.md), конфиг умолчания (paths.specs=docs/specs, paths.steps=docs/specs/steps)
решение оркестратора: стоящая инструкция автора для UI-шагов (05, 06 и любых порций с интерфейсом) — вёрстка должна соответствовать docs/prototype (экраны BoardLight, CardStates, Main, NewTask, TaskPanel), всё на Vue, ui-kit — @zoloto585/facet (уже зафиксирован в CLAUDE.md проекта); работу с ui-kit делать одной из первых порций соответствующего шага и показать автору результат, не останавливая конвейер — чем плохо, если забыть: экран разъедется с прототипом или использует не тот kit — переделка всего шага
шаг 01: вопрос — релевантность чанков для s3/s4, нужен ли tests/, что собирать в diff/worktree metadata для s4
шаг 01: ответ автора — слоями (чек-лист+heading-match+FTS), завести tests/ на unittest, сервер сам вызывает git в tasks.worktree — все три варианта предпочтения агента
шаг 01: старт, порций 5 (a: тесты+чанкер, b: статус документа, c: поиск/embeddings по чанкам, d: контракт context, e: документация+MCP). Разведка выявила: шаг 01 уже частично реализован черновиком в коммите 3452012 — порции чинят разрывы с контрактом, а не пишут с нуля
порция a: критика — 3 блокирующих (Б1 конфликт overlap/границ списка, Б2 инвариант размера чанка нарушается overlap'ом, Б3 процедура «красный до правки» невыполнима), принято 3, ТЗ поправлено
порция a: готово (исполнитель), открытых вопросов нет, доказательство «красный до правки» приложено
порция a: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-01.diff-a.r1.txt
порция a: готово (коммит 3ab8a7a)
порция b: критика — 2 блокирующих (Б1 context() отбрасывает документ с сохранёнными чанками пропавшего файла, Б2 checked_at ломает побайтную стабильность context), принято 2, ТЗ поправлено
порция b: готово (исполнитель), открытых вопросов нет
порция b: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-01.diff-b.r1.txt
порция b: готово (коммит e6b20f5)
порция b: отложено — delete_project не чистит document_chunk_fts/embeddings/documents при удалении проекта с задачами (тот же дефект, что порция b починила для delete_task) — кандидат в отдельную задачу вне этого шага
порция c: критика — 4 блокирующих (Б1 chunk_sql не отдаёт path/start_line/end_line, Б2 info-словарь перетирает лексический хит векторным, Б3 тесты по векторам зелены при любой реализации, Б4 OLLAMA_URL читается на импорте — env-переменная не сработает), принято 4, ТЗ поправлено
решение оркестратора: стоящая инструкция автора — сверяться с docs/listik-vision.md по объёму и духу продукта; не делать лишнего сверх того, что там и в спеке (например, отдельных «скилов»); мобильная версия (шаг 06 limited-mobile-view) должна быть максимально облегчённой — только посмотреть задачи, без лишнего функционала — чем плохо, если забыть: конвейер нарастит объём, который потом придётся выкидывать
порция c: готово (исполнитель), открытые вопросы — двойной вызов Ollama в hybrid вне границ порции (компромисс: тест через vector() напрямую), web/client.ts не подхватит новый дефолт kinds (вне границ, наблюдение)
порция c: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-01.diff-c.r1.txt
порция c: готово (коммит 6e033e5)
порция d: критика — 4 блокирующих (Б1 with_details=True тащит все comments/events/documents вопреки урезке по этапам, Б2 dependencies без stable() ломает побайтную стабильность, Б3 journal смешивает комментарии/события без общего ключа сортировки ts, Б4 тест s4-worktree требует HEAD в репо без начального коммита), принято 4, ТЗ поправляется
решение оркестратора: с этого момента блокирующие замечания критика принимаю сам без AskUserQuestion (автор сказал «принимай от критика все блокирующие сам, я хочу готовое увидеть, считаю информации достаточно») — вопросы этапов 1 и 3 (по существу продукта) продолжаю передавать автору дословно — чем плохо, если забыть: лишние остановки конвейера на решения, которые автор уже делегировал
порция d: ТЗ поправлено (Б1-Б4 приняты)
порция e: критика — 3 блокирующих (Б1 checked_at описан в документации, но его нет в GET /api/tasks/{id}, Б2 ТЗ требует валидации stage у context, которой нет и которую d не делает, Б3 нет предусловия «порция d закоммичена» — при старте до коммита d пункт чек-листа пройдёт на старой форме ответа), решение оркестратора: принято 3/3 автоматически (стоящее указание автора)
порция e: ТЗ поправлено (Б1-Б3 приняты)
порция d: готово (исполнитель), открытые вопросы — тексты reasons[] сформулированы сам, «блок включён» определён по наличию содержимого, унаследованная особенность textutil.clip не чинилась (вне границ)
порция d: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-01.diff-d.r1.txt
порция d: готово (коммит 39787d7)
порция d: отложено — 3 незамеченных пункта судьи (suggested[] безусловно [] на s3/s4, слои 3-4 не останавливаются по лимиту досрочно, --format text печатает worktree как repr словаря) — не блокировали приёмку
порция e: готово (исполнитель), открытых вопросов нет
порция e: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-01.diff-e.r1.txt
порция e: красное 1/2 — API.md утверждает, что show переиндексирует (неверно, зафиксировано порцией b), README.md/mcp.py утверждают, что verdict/journal/worktree заполняются на s3 (неверно, только s4-judge, расходится и с кодом, и с собственным API.md порции)
порция e: повтор — готово (исполнитель), оба красных пункта закрыты точечной правкой текста
порция e: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-01.diff-e.r2.txt (предыдущий: step-01.diff-e.r1.txt)
порция e: готово (коммит 1b8ca20)
шаг 01: закрыт, порций 5/5
шаг 01: бумаги закоммичены (48f84c3), пуш пропущен — у репозитория нет remote
шаг 02: вопрос — критерий «needs-owner теряет повторный вопрос», править ли код
шаг 02: ответ автора — (б) кодовая порция: question/answer как комментарий, HTTP+MCP, с тестом (предпочтение агента)
шаг 02: старт, порций 3 (a: needs-owner код, b: канонический протокол harness-protocol.md, c: документация)
шаг 02: отложено — stage --to с пустого этапа считается handoff по умолчанию (None:s2-review нет в таблице переходов) и снимает держателя — кандидат в отдельную задачу, ближе к шагу 04
порция a: критика — 2 блокирующих (Б1 вариант «через update_task с подавлением события» противоречит границам правки — гарантированный провал приёмки, Б2 ложная предпосылка про паттерн CLI-теста через subprocess — такого паттерна в репозитории нет), решение оркестратора: принято 2/2 автоматически
порция a: ТЗ поправлено (Б1-Б2 приняты)
порция a: готово (исполнитель), открытых вопросов нет
порция a: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-02.diff-a.r1.txt
порция a: готово (коммит b1ab7a7)
порция b: критика — 3 блокирующих (Б1 протокол не задаёт смену держателя на sticky-переходах, Б2 требование про первое слово вердикта недостаточно — add_comment ищет «красн»/red/fail в любом месте текста, Б3 ложная предпосылка — context на s3-impl не отдаёт вердикт/журнал), решение оркестратора: принято 3/3 автоматически
порция b: ТЗ поправлено (Б1-Б3 приняты)
порция b: готово (исполнитель), открытых вопросов нет
порция b: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-02.diff-b.r1.txt
порция c: критика — 3 блокирующих (Б1 ложная предпосылка — раздел README не там где указывает ТЗ, Б2 пример init-projects без --dry-run пишет в чужие репозитории, Б3 проверка «только три файла» невыполнима — в дереве уже изменён журнал и лежат бумаги шага), решение оркестратора: принято 3/3 автоматически
порция b: готово (коммит 640c4c5)
порция c: готово (исполнитель), открытых вопросов нет
порция c: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-02.diff-c.r1.txt
решение оркестратора: проверено — отдельного шага API-контракта для UI нет и не нужен, API (client.ts) уже покрывает шаги 05/06 полностью; общих файлов между бэкенд-шагами (listik/*.py, tests/) и UI (web/*) нет — завожу трек ui (шаги 05+06) параллельно основному дереву (шаги 03, 04, 07)
шаг 04: заметка на будущее (от разведки) — п.6 «sticky/handoff-переходы и окно возврата после красного verdict» может частично пересекаться с уже сделанным в шаге 02.b (docs/harness-protocol.md, stage --holder) — учесть при нарезке порций шага 04
трек ui: шаг 05, дерево /Users/dmitry.fomin/Projects/Listik-ui, ветка pipeline-ui, база 640c4c5
порция c: готово (коммит 2583e12)
шаг 02: закрыт, порций 3/3
шаг 02: бумаги закоммичены (c0715ef), пуш пропущен — у репозитория нет remote
шаг 03: вопрос — входной формат (JSON-only vs JSON+Markdown), slug проекта, прогон на живой базе — часть шага или нет
шаг 03: ответ автора — А (только JSON/JSONL, предпочтение агента), В (slug=writerllm, НЕ предпочтение агента — агент предпочитал WriterLLM), Б (прогон на живой базе — последняя порция шага, НЕ предпочтение агента — агент предпочитал не делать это частью шага)
шаг 03: старт, порций 4 (a: import_writerllm.py переписан, b: --update+отчёт, c: документация, d: прогон на живой базе ~/Agents/WriterLLM)
порция a: критика — 2 блокирующих (Б1 комментарий без created_at дублируется при повторном импорте — не срабатывает дедуп, Б2 противоречие про export.json — запись без _type станет задачей вместо memory, тест на «тот же набор задач/рёбер» ложен), решение оркестратора: принято 2/2 автоматически
порция a: ТЗ поправлено (Б1-Б2 приняты)
решение оркестратора: критик также отметил 2 существенных (не блокирующих) — С3 actors.remember/recompute_blocked пишут в базу даже в dry-run (противоречие с §6), С9 LISTIK_DB не префиксом на каждой команде — риск записи в живую базу судьёй. Принимаю оба сам сверх блокирующих — второе прямо касается риска повредить боевые данные — чем плохо, если забыть: судья случайно запишет тестовые данные в реальный listik.db
порция a: готово (исполнитель), открытые вопросы — приоритет по умолчанию 2 без warning при полном отсутствии поля (симметрично статусу), dependencies в dry-run считает проверку на существующее ребро (безопасно для SELECT)
порция a: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-03.diff-a.r1.txt
порция a: готово (коммит 4fce07a)
порция b: критика — 3 блокирующих (Б1 фикстура export.jsonl всегда даёт ошибку dependency — ok=True недостижим, Б2 чек-лист сверяется с несуществующей _task_fields(partial=True) и устаревшим утверждением про --update, Б3 требование «started_at/closed_at не меняются» противоречит update_task, а его трогать нельзя), решение оркестратора: принято 3/3 автоматически
порция b: ТЗ поправлено (Б1-Б3 приняты); заметка на будущее — С9 устаревшая справка CLI «--source ... или каталог Markdown» (Markdown-адаптер уже удалён порцией a) — учесть в порции c (документация)
порция b: готово (исполнитель), открытых вопросов нет
порция b: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-03.diff-b.r1.txt
порция b: готово (коммит 94ac384)
порция b: отложено — help подпарсера import-writerllm не показывает фразу «работает с базой напрямую» на --help этой команды (только на общем), не блокировало приёмку — можно закрыть одним description= на приёмке шага
порция c: критика — 1 блокирующее (ТЗ требует «каждый пример прогнать без ошибок», но export.jsonl всегда даёт ok=false/код 1 из-за WL-missing, это тот же факт из ревью порции b), решение оркестратора: принято 1/1 автоматически
порция c: ТЗ поправлено (Б1 принято, отложенный help description= из порции b тоже включён)
решение оркестратора: попутная мелочь (З14) — --source описан как «JSON/JSONL файл или каталог Markdown» в той же строке, что уже правится (description= подпарсера) — включаю в ту же правку, раз строка всё равно трогается
порция c: готово (исполнитель), открытых вопросов нет
порция c: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-03.diff-c.r1.txt
порция c: красное 1/2 — п.15 чек-листа требует "import-writerllm" во всех трёх файлах (README/API/CLAUDE), но CLAUDE.md — 0 совпадений; в CLAUDE.md:78-83 назван только модуль import_writerllm.py и флаг --update, имени команды нет
порция c: повтор — готово (исполнитель), красный пункт закрыт точечной правкой CLAUDE.md
порция c: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-03.diff-c.r2.txt (предыдущий: step-03.diff-c.r1.txt)
порция c: готово (коммит 9d85598)
порция d: критика — 4 блокирующих (Б1 повтор импорта перезаписывает import_note на «0 задач» — пункт 9 провалится на корректном прогоне, Б2 N=wc -l выгрузки вместо числа issue-записей — ignored≠0 сломает проверку, Б3 нет бэкапа базы/отката перед необратимой записью — --remove не чистит embeddings/actor_aliases, Б4 имя/e-mail владельца необратимо осядут в tasks.assignee/comments.author/actor_aliases через actors.resolve — нужна read-only проверка акторов до записи), решение оркестратора: принято 4/4 автоматически
порция d: ТЗ поправлено (Б1-Б4 приняты, плюс С10 про spec_path/journal_path); запускаю реализацию — это необратимая запись 777 задач с личными данными владельца в живую базу, ТЗ требует бэкап перед записью и read-only проверку акторов с остановкой при несведённых ключах
порция d: готово (исполнитель) — 777 задач, 722 связи, 90 комментариев импортированы, 0 ошибок, 0 несведённых акторов, снимок базы сделан до записи; репозиторий не тронут (git status чист), артефакты в $WORK=~/listik-import-writerllm-2026-09-12
порция d: готово (операция принята, без коммита — репозиторий не менялся, HEAD 9d85598)
шаг 03: перенесено на приёмку шага — среди импортированных связей есть dep_type related(3) и supersedes(1), которых нет в номенклатуре listik/deps.py — проверить, как ведут себя в расчёте «заблокирована» и на доске
шаг 03: закрыт, порций 4/4
шаг 03: бумаги закоммичены (1aa4128), пуш пропущен — у репозитория нет remote
шаг 04: вопрос — область worktree-лока (какие этапы лочат дерево), проверяет ли claim таблицу разрешённых harness
шаг 04: ответ автора — лочат только s3/s4 (предпочтение агента), claim фильтр только через ready (предпочтение агента)
шаг 04: старт, порций 4 (a: зависимости/suggested-blocks, b: маршрутизация/routing, c: claim+окно возврата, d: документация)
порция a: критика — 2 блокирующих (Б1 MCP listik_deps без actor обходит подтверждение — actors.resolve(None) даёт unknown=человек, Б2 show даст KeyError на висячей мягкой связи — dep_title отсутствует у missing-целей, в живой базе уже 16 таких строк), решение оркестратора: принято 2/2 автоматически
порция a: ТЗ поправлено (Б1-Б2 приняты, фикс Б2 расширен на cmd_dep_tree — та же ошибка в соседнем месте, принято молча)
порция a: готово (исполнитель), открытых вопросов нет
порция a: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-04.diff-a.r1.txt
порция a: готово (коммит 40e4322)
порция a: отложено — предсуществующий (на HEAD) дефект deps.py:357: dep tree на висячей ЖЁСТКОЙ связи даёт KeyError: 'holder_title' (аналог уже починенной Б2, но для другого типа связи) — кандидат в будущую порцию, не блокировало приёмку
порция b: критика — 3 блокирующих (Б1 --local не доходит до хелперов projects — CLI-сценарии уйдут по HTTP в реальную БД, Б2 тесты claim невыполнимы — держатель/лок worktree конфликтуют без release/разных проектов, Б3 routing_effective/routing_source не заданы для PATCH /api/projects/{slug} — update_project их не возвращает), решение оркестратора: принято 3/3 автоматически
порция b: ТЗ поправлено (Б1-Б3 приняты)
порция b: готово (исполнитель), открытых вопросов нет
порция b: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-04.diff-b.r1.txt
порция b: готово (коммит 478c2b8)
порция c: критика — 2 блокирующих (Б1 противоречие: §3 требует повторный claim обновлять holder_at, §1.4 требует «без изменений и без события» — идемпотентная ветка сейчас не трогает holder_at вовсе, ни тесты ни чек-лист это не ловят, Б2 чек-лист п.24 HTTP нечем безопасно выполнить — без явных базы/порта уйдёт в реальную listik.db или столкнётся портом с рабочим сервером), решение оркестратора: принято 2/2 автоматически
порция c: ТЗ поправлено (Б1-Б2 приняты); решение оркестратора — попутно принимаю С3 (перечитать строку после expire_return_handoffs, иначе сценарий §5.12 упадёт на устаревшей row) — прямо следует из логики только что принятой Б1
порция c: готово (исполнитель), открытых вопросов нет
порция c: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-04.diff-c.r1.txt
порция c: готово (коммит b9c04e3)
порция d: критика — 4 блокирующих (Б1 правка harness-protocol.md ломает test_ten_rules_match_spec_verbatim — требование невыполнимо при запрете трогать docs/specs и tests, Б2 init-projects --only listik не обновит AGENTS.md на пустой базе + утверждение «только AGENTS.md» ложно — CLAUDE.md тоже в TARGETS, Б3 описание harness у listik_ready лежит внутри inputSchema, а границы требуют inputSchema без изменений — противоречие, Б4 ложная предпосылка про однострочный формат ошибок — при сервере «ошибка <код>: …», claim многострочный), решение оркестратора: принято 4/4 автоматически
порция d: ТЗ поправлено (Б1-Б4 приняты)
порция c: ТЗ поправлено (Б1-Б3 приняты)
решение оркестратора: автор попросил не начинать шаги 06 и 07 — довожу до конца текущие порции (04.d в основном дереве, 05.d в треке ui), закрываю шаги 04 и 05, дальше СТОП без разведки/нарезки 06/07 до новых указаний
порция d: НАХОДКА — migrate.upsert(CLAUDE.md) испортил файл: маркеры BEGIN/END LISTIK ищутся как подстрока где угодно в тексте, а не как отдельная строка; в CLAUDE.md реального блока нет, только упоминание маркеров в прозе — upsert принял это за блок и заменил весь текст между совпадениями. Исполнитель сразу заметил (git diff --stat +112 строк), откатил git checkout -- CLAUDE.md (файл был чист на старте задачи, до коммита порции, ничего не потеряно), внёс нужные правки руками без повторного вызова migrate на CLAUDE.md. Реальный баг migrate.py вне границ порции — кандидат в отдельную задачу (искать маркеры только как самостоятельные строки ^<!-- BEGIN LISTIK -->$)
порция d: расхождение — «Как проверить» ожидало dry_run=True → unchanged для обоих файлов, но CLAUDE.md даёт updated (ожидаемо из-за найденного бага, AGENTS.md — unchanged, там маркеры настоящие)
порция d: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-04.diff-d.r1.txt
порция d: готово (коммит ba75acb) — судья подтвердил баг migrate.py (маркеры ищутся подстрокой) и нашёл ещё один: bin/listik cmd_context текстовый вывод показывает только dependencies.hard, suggested не виден в тексте (--format json показывает верно) — оба вне границ порции a, кандидаты в отдельные задачи
шаг 04: закрыт, порций 4/4
решение оркестратора: по просьбе автора собрал 6 найденных по ходу конвейера мелких багов (delete_project cleanup, deps.graph KeyError, cmd_context text worktree/suggested, transition_kind для пустого этапа, migrate.py substring-маркеры) и починил одним заходом сам (без конвейера — каждый маленький и уже диагностирован), с регрессионными тестами (9 новых, 180/180 зелёных), коммит 46429e2
решение оркестратора: ОШИБКА исправлена — шаг 05 (трек ui) не был закрыт, порция e (NewTask) ещё не сделана; «не начинай 6 и 7» относится к новым шагам, не к недоделанной порции уже начатого шага 05 — продолжаю трек ui порцией e, шаг 04 в основном дереве остаётся закрытым по факту (4/4)
шаг 05: закрыт по-настоящему, порций 5/5, бумаги закоммичены (8bc3e09)
трек ui: сведён в основное дерево (merge --no-ff 38d2ccc, без конфликтов), worktree Listik-ui и ветка pipeline-ui убраны; полная проверка после сведения — 180 python-тестов OK, npm typecheck/build чисто
шаг 06: НЕ начат по решению автора (см. выше) — трек ui закрыт только с шагом 05
решение оркестратора: автор — шаг 07 (codex-launch-from-claude) уже реализован отдельно как плагин ~/Agents/claude/codex-plugin-claude-code (skills codex-delegate/codex-check/codex-jobs/codex-second-opinion/codex-runtime, agents/codex-runner.md); явной интеграции с Listik там не нашёл (grep "listik" по README/skills/agents/scripts — пусто), но по указанию автора шаг 07 в конвейер не беру, считаю сделанным вне Listik
шаг 06: старт по указанию автора — трека/worktree не завожу (шаг 07 больше не претендует на параллельный main-tree слот, работаю в основном дереве последовательно)
шаг 06: вопрос — действия в карточке на телефоне (только просмотр vs +ответить vs claim/comment/needs-owner по спеке), что над списком на первом экране (ничего лишнего vs +компактный инбокс vs всё как сейчас), откуда данные (свой пагинированный /api/tasks vs /api/board целиком как сейчас)
шаг 06: ответ автора — чистый просмотр без действий, только фильтр проекта+поиск над списком, свой пагинированный GET /api/tasks — все три предпочтения агента
шаг 06: старт, порций 3 (a: инструменты+планшет+MobileTaskRow, b: режим телефона — очередь по страницам, c: карточка только для чтения)
порция a: критика — 3 блокирующих (Б1 order=priority противоречит правилу priority ASC, updated_at DESC из самого ТЗ, Б2 ложная предпосылка «до правки панель 720» — глобальное правило уже даёт min(720,100vw), проверка не может упасть, Б3 блок phone считается при открытой панели задачи — метрика негодна, дровер кита закрывает экран inert+overflow:hidden), решение оркестратора: принято 3/3 автоматически
порция a: ТЗ поправлено (Б1-Б3 приняты); попутно принимаю С8 (noUnusedLocals в tsconfig уронит typecheck после выноса строки из MobileTaskList.vue, граница «не улучшать» может читаться как запрет убирать осиротевшие импорты) — уточняю явно, чтобы не терять круг на первом же прогоне
порция a: готово (исполнитель), открытые вопросы — 1) innerWidth≠width на самых узких ширинах (<376px) в headless Chrome — экологическая особенность машины при mobile:true, принял решение исполнителя оставить буквально по ТЗ; 2) СЕРЬЁЗНОЕ — phone.containerWidth (метрика из моего же фикса Б2) не работает: глобальное правило .listik-shell>.ui-container с !important перебивает узкие медиа-блоки независимо от ширины экрана — предсуществующий баг каскада CSS, вне границ порции a. Нужна другая проверка порога 767px, отправляю на ещё один заход правки ТЗ
порция a: ТЗ поправлено — верификация 767px через bodyOverflowX+compactPillVisible вместо containerWidth
решение оркестратора: !important-баг даёт на телефоне боковые поля 32px вместо 12px — прямо противоречит инструкции автора про максимально лёгкую мобильную версию. Раз порция b всё равно стилизует контейнер телефона, включаю починку туда
порция a: код обновлён под новую верификацию (bodyOverflowX/compactPillVisible), проверено вживую на 700px — совпадает с ожиданием
порция a: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-06.diff-a.r1.txt
порция b: ТЗ поправлено — !important-фикс дублируется в оба медиа-блока (767 и 900px, планшет 768-900 тоже чинится как побочный эффект того же бага, принято молча — не расширение объёма, а починка), новое поле phone.containerBox для проверки
порция a: готово (коммит 32e0ec5)
шаг 06: перенесено на приёмку шага — innerWidth=376 вместо 360 на самой узкой ширине (контент не влезает, Chrome раздвигает viewport); порция a уменьшила переполнение (было 380 на HEAD), полное закрытие ожидается от порции b (телефонная вёрстка)
порция b: критика — 2 блокирующих (Б1 п.5 противоречит п.8a про 376 — нужно требование «контент влезает в 360» и расчёт от clientWidth, Б2 п.13 «все проекты» нечем выполнить — UiSelect кита не сбрасывает в null, нужен явный пункт {value:'',label:'все проекты'} в B3.1), решение оркестратора: принято 2/2 автоматически
порция b: ТЗ поправлено (Б1-Б2 приняты); 3 существенных (requests-счётчик вне Runtime.evaluate, containerBox абсолютные ожидания при скроллбаре, PHONE_PAGE не экспортируется из script setup) не принимал — решаемо исполнителем в рамках границ (второй <script> блок для константы)
порция b: готово (исполнитель), открытый вопрос — «5 строк» на 800px в тексте порции, видимо, отсылка к MobileTaskList без --fill (не в зоне правки b), с --fill=50 у неё 24 строки — не дефект, планшетный компонент не менялся
порция b: ИНЦИДЕНТ (3-й раз подряд) — снова git stash -u/pop без подтверждения; проверено: дерево в порядке, потерь нет, все 7 файлов на месте (фидбек уже отправлен после 2-го случая, повторно не дублирую)
порция b: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-06.diff-b.r1.txt
порция b: красное 1/2 — пп.8/9 queueBadge/moreLabel в smoke.mjs измеряются ПОСЛЕ клика по «Показать ещё» вместо ДО (smoke.mjs:259-292 — badgeEl/moreEl запоминаются, но innerText читается уже после клика+900мс); первая страница очереди этим инструментом не измеряется вовсе
(попутно: убрал зависший worktree-эталон судьи в scratchpad/base, force-remove, не относится к порции)
порция b: повтор — готово (исполнитель), красный пункт закрыт
порция b: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-06.diff-b.r2.txt (предыдущий: step-06.diff-b.r1.txt)
порция b: готово (коммит bbafcb6) — упомянутый судьёй хвост worktree-эталона проверен, уже пуст (git worktree list — только main), не проблема
порция c: критика — 1 блокирующее (п.13 чек-листа опирается на неверные факты — fillTask() не даёт пустой acceptance, listik-fill-010 не «open без держателя», нужна замена на listik-fill-011 и перенос проверки пустых критериев на живой сервер), решение оркестратора: принято 1/1 автоматически
порция c: ТЗ поправлено (Б1 принято)
порция c: готово (исполнитель), ручные DevTools-проверки не выполнены (требуют интерактива/временной базы) — автоматика зелёная, поручить судье
порция c: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-06.diff-c.r1.txt
порция c: красное 1/2 — п.17 длинный заголовок (65 символов без пробелов) расширяет .ui-drawer (scrollWidth 738 вместо 360): overflow-wrap:anywhere в app.css:149-151 покрывает только .listik-phone-sheet, а h2.listik-drawer__title в слоте #header (PhoneTaskSheet.vue:103) и .ui-drawer__header — вне этого правила
порция c: повтор — готово (исполнитель), красный пункт закрыт (класс listik-phone-sheet__head + overflow-wrap:anywhere на шапке)
порция c: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-06.diff-c.r2.txt (предыдущий: step-06.diff-c.r1.txt)
порция c: готово (коммит ab3f567)
шаг 06: закрыт, порций 3/3
