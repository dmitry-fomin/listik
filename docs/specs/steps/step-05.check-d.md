# Приёмка порции 05.d. Панель задачи по TaskPanel

Проверки — в `/Users/dmitry.fomin/Projects/Listik-ui/web` на моке (`scripts/mock-api.mjs`) и на
живом сервере с временной базой по сценарию из ТЗ (задача прошла s1→s2 sticky, s2→s3 handoff,
есть review-комментарий и вопрос автору).

## Сборка и границы

1. `npm run typecheck`, `npm run build` зелёные; `git status --porcelain` — только `TaskDrawer.vue`,
   `App.vue`, `api/types.ts`, `assets/app.css`, `lib/format.ts` (если менялся), `scripts/smoke.mjs`,
   `web/README.md`. `store/listik.ts`, `api/client.ts`, `lib/health.ts`, `scripts/mock-api.mjs`,
   доска, инбокс, тулбар, `node_modules` — без изменений.
1a. Дифф `api/types.ts` — только добавления: в `Task` появились опциональные `checklist_path`,
   `review_path`, `decision_path`; новый `TaskDocument`; в `TaskDetail` — `documents?: TaskDocument[]`;
   ни одна существующая строка не удалена и не изменена. **Красный до правки**: `vue-tsc` падает на
   `task.checklist_path`.
2. `grep -n "UiSwitch\|UiPopover\|Быстрые переключатели\|События задачи\|Перевести на этап\|заметка к переходу" web/src/components/TaskDrawer.vue`
   пуст. **Красный до правки**: всё это есть. `grep -n "body: { stage }" web/src/components/TaskDrawer.vue`
   пуст (прямого PATCH этапа нет).

## Шапка

3. Открыть `listik-web-a1b2` (мок, `idle_hours: 0.3` ≥ порога 0.25 → `at-risk`): в шапке —
   иконка типа, метка проекта «Li · Listik», заголовок задачи `h2`, пилюля здоровья тона
   `at-risk` с текстом «под угрозой · молчит 18 мин», пилюля статуса «в работе», иконка приоритета
   и слово «обычный», бейдж `info` «frontend» (метка), справа моноширинный id с кнопкой
   копирования. Тексты пилюли без дубля слова: `listik-sse-e5f6` (`dead`) — «брошена 40 ч» (не
   «брошена · брошена 40 ч»); `listik-api-c3d4` (`abandoned` без держателя) — «брошена · без
   держателя»; `listik-epic-k9l0` (`unknown`) — «без держателя» (не «без держателя · без
   держателя»); на живом сервере задача со свежим heartbeat — «здорова · hb …», закрытая —
   «закрыта». Для `listik-metrics-g7h8` — бейдж `warning` «ждёт 1» и подпись «· порция шага
   «Эпик: слой зависимостей на доске»» (по `deps_state.parent`). `lib/health.ts` не изменён.
4. Ширина `.ui-drawer` при 1440 — 720px (правило не потеряно).

## «Где стоит процесс»

5. `UiSteps` из пяти шагов: подписи `s1 · ТЗ и чек-лист`, `s2 · Второе мнение`, `s3 · Реализация`,
   `s4 · Проверка и коммит`, `done`. Живой сервер (сценарий ТЗ: `claim --holder agent:claude` на
   s1, `stage` ×2): s1 `done` «claude · <длительность> · sticky →» — «claude» взят из `to_value`
   события `claim` в окне s1 (у события `stage` актора нет — сервер его не пишет); s2 `done`
   «claude · <длительность> · handoff →» — унаследован по sticky-переходу в s2 (в окне s2 нет
   `claim`/`heartbeat`); s3 `current` «без держателя · <возраст> · сейчас», s4 «та же сессия»,
   done «коммит судьи». Мок `listik-web-a1b2` (события: `claim` dsh, затем `stage` s2→s3): s2
   `done` «dsh · 1 ч 30 мин · handoff →», s3 `current` «dsh · 2 ч · сейчас»; s1 `done` «— · sticky →»
   (события входа в s1 нет). Задача без этапа (эпик на моке): все шаги `upcoming`, подсказка «вне
   конвейера». **Красный до правки**: подписи шагов — только код этапа, длительностей нет.
   Реализация, читающая `event.actor` события `stage`, даст «—» везде — красный пункт.
6. Справа от заголовка — «на s3 <возраст>»; у задачи со `stage_warn` — «· дольше порога».
7. Ряд действий: «Heartbeat», «Нужен автор» (или «Снять «нужен автор»» при флаге), «Следующий этап»,
   «Освободить», `UiSplitButton` «Взять в работу» с меню из трёх пунктов: «Взять всё равно (обход
   блокеров)» (danger), «Закрыть с результатом…», «Добавить связь…». У `.ui-split-button` **ни в
   одном состоянии** нет атрибута `disabled` на главной кнопке и стрелке (кроме `loading` во
   время `claim`): стрелка меню открывается и у задачи, которую брать нельзя. Отдельных кнопок
   «Взять всё равно», «Закрыть с результатом», поля «ID блокера» до выбора пункта меню нет.
   **Красный до правки / при `:disabled="!claimable"`**: у заблокированной задачи стрелка меню
   погашена и «Взять всё равно» недостижимо.
8. `listik-web-a1b2` (держит dsh, `deps_state.ready: false`, `claimable: true`): обёртка кнопки
   `data-can-claim="false"`; клик по главной кнопке не шлёт `POST …/claim` (`MOCK_LOG=1`), а
   раскрывает `UiAlert warning` «Взять нельзя» с причиной «держит dsh (18 мин)»; подсказка под
   рядом — «взять нельзя: держит dsh (18 мин), переход s3 → s4 sticky — следующий этап идёт в
   той же сессии»; пункт меню «Взять всё равно» `disabled` (задача занята, а не заблокирована).
   `listik-metrics-g7h8` (блокер, держателя нет, `claimable: false`): `data-can-claim="false"`,
   подсказка «взять нельзя: ждёт завершения …»; пункт «Взять всё равно» активен →
   `UiConfirmDialog` danger → подтверждение шлёт `claim` с `force: true` (мок отвечает 200; без
   `force` мок отвечал бы 400). `listik-api-c3d4`/`listik-epic-k9l0` (свободны, `ready: true`):
   `data-can-claim="true"`, «можно брать прямо сейчас», клик раскрывает выбор держателя +
   «Подтвердить» → `POST …/claim`. Живой сервер: `claim` занятой другим задачи (вручную
   отправить «Подтвердить» после подмены `data-can-claim` в devtools) отвечает 400 «уже
   удерживается …» и показывается `toast.danger`.
9. «Нужен автор» раскрывает поле «вопрос автору» + «Отправить вопрос» → `POST …/needs-owner
   {value:true, note}`; тост «Вопрос автору записан»; на живом сервере в ленте появляется запись
   «вопрос · <автор>», карточка попадает в инбокс. «Снять» при флаге шлёт `{value:false}` без текста.
10. «Следующий этап» → `POST …/stage {holder}`; «Heartbeat» → `POST …/heartbeat`; «Освободить» →
    `POST …/release`; каждая ошибка сервера (остановить мок посреди действия или отдать 400) даёт
    `toast.danger` с текстом ошибки, а успех — обновление панели и доски (`refresh` + `reloadDetail`).

## «Холодный старт»

11. Семь строк `.listik-cold__row` в порядке `spec_path, acceptance, journal_path, worktree · branch,
    blocks, comment -k journal, comment -k review`; бейдж «N из 7» (`success` только при 7).
    Мок `listik-web-a1b2`: `spec_path` ок (`docs/listik-board.md`), `acceptance` ок (первая
    строка текста), `journal_path` ок, `worktree · branch` ок, `blocks` «ничего не ждёт», `comment -k
    journal` ок («взял в работу»), `comment -k review` — предупреждение «ревью нет» → «6 из 7»,
    бейдж `warning`; мок не отдаёт `checklist_path`/`review_path`/`decision_path` — при
    `undefined` строки считаются по `acceptance`/`journal_path`/комментариям без ошибок в консоли.
    Живой сервер по сценарию: `worktree` — «рабочее дерево не указано», `comment -k review` —
    «1 замечаний»; задача, созданная с `--checklist docs/x.md`, показывает в строке `acceptance`
    этот путь. Значение пути копируется кнопкой. **Красный до правки**: секции нет.

## «Кто держит»

12. Строки: держит (иконка харнесса + «dsh · 18 мин»), heartbeat (дата · «18 мин назад»), что
    делает («пишу панель задачи»), этап с (дата · «2 ч»); «исполнитель» появляется только когда
    `assignee` задан и ≠ `holder`. Строк «обновлена/начата/закрыта/источник/worktree/spec» нет.

## «Журнал и вердикты»

13. Сегмент «всё / журнал / ревью / вердикт»; бейдж-счётчик меняется с фильтром. Мок: «всё» —
    комментарии `journal`, `verdict` и события `stage`, `claim` (4); «журнал» — 1 комментарий + 1
    событие `stage`; «вердикт» — 1 (тон accent; вердикт с текстом «красный …» на живом сервере —
    тон danger); «ревью» — пусто с `UiEmptyState`. Заголовок события stage — «stage s2-review →
    s3-impl · handoff» (на моке note без типа — без суффикса), heartbeat — «heartbeat · <actor>».
14. Строка ввода: `UiSelect` вида (по умолчанию «журнал»), `UiInput`, «Отправить»; `Enter` в поле
    отправляет. Вид «журнал» → `POST …/comment {kind:'journal'}`; вид «ответ» → `POST …/needs-owner
    {value:false, note}` и тост «Ответ отправлен» (на живом сервере: флаг снят, карточка уходит из
    инбокса, в ленте «ответ · me»); вид «вопрос» → `needs-owner {value:true, note}`.
    **Красный до правки**: вид «ответ» отправляет обычный комментарий и флаг не снимает.
15. Кнопка «Ответить» на карточке инбокса открывает панель, в строке ввода выбран вид «ответ» и
    фокус стоит в поле (`document.activeElement` — этот `input`).

## «Связи»

16. `listik-metrics-g7h8`: подзаголовок «Ждёт завершения», карточка `listik-api-c3d4` с бейджами
    «блокирует», `s1`, подсказкой «без держателя · стоит без движения» (мок: `stale` false → «без
    держателя»), `UiAlert warning` «Блокеры стоят без движения» (блокер без держателя); строка
    «родитель: listik-epic-k9l0 …»; «связано, не блокирует: listik-sse-e5f6». Клик по id открывает
    ту задачу в той же панели. `listik-api-c3d4`: «Ждут её завершения» с `listik-metrics-g7h8`.
17. «Дерево связей» грузит `POST …/deps {depth:3}` и показывает список; «Добавить связь…» из меню
    раскрывает поле id + кнопку → `POST …/deps {depends_on}`; ошибка (несуществующий id на живом
    сервере) — `toast.danger`.

## Описание

18. Последняя секция: «Описание · ТЗ» и «Критерии приёмки» всегда; «Дизайн»/«Заметки»/«Результат»
    — только при непустых значениях (на моке `result` пуст → заголовка «Результат» нет).

## Раскладка и не сломано

19. `npm run smoke -- http://localhost:5173/ 1440`: `drawer.открылась`, `шагов === 5`,
    `coldRows === 7`, `splitButton === true`, есть тексты пяти секций, консоль чистая; при 640 в
    панели скрыты ряд действий процесса и форма закрытия (классы `listik-drawer__process`,
    `__close-action` присутствуют на новых блоках).
20. Фильтры доски (чип, проект) после закрытия панели не сброшены; панель закрывается по Esc и по
    клику на подложку.
21. `web/README.md` описывает меню `UiSplitButton`, «Холодный старт» и ответ автору из панели.
22. В диффе нет токенов и содержимого `config.toml`.
