трек ui: шаг 05, дерево /Users/dmitry.fomin/Projects/Listik-ui, ветка pipeline-ui, база 640c4c5
шаг 05: вопрос — 8 пунктов (DnD карточек, переключатель видов, фильтр харнесса, строка харнессов в шапке колонки, метка/цвет проекта, NewTask-оценщик/матрица+декомпозиция, панель фильтров, как показать порцию кита)
шаг 05: ответ автора (пакет 1/2) — убрать DnD совсем (предпочтение агента), UiTabs (предпочтение агента), фильтр харнесса — не делать до шага 04 (НЕ предпочтение агента), строка харнессов в шапке — показывать харнесы держателей (НЕ предпочтение агента)
шаг 05: ответ автора (пакет 2/2) — метка/цвет из API (предпочтение агента), NewTask: форма без оценки + матрица харнесс×процесс с выбором в labels (частично НЕ предпочтение агента — агент предпочитал без матрицы вовсе), UiFilterBar только во вкладке «Список» (предпочтение агента), показ порции кита: документ+примитивы+витрина sheet.html (предпочтение агента)
шаг 05: старт, порций 5 (a: кит+примитивы+витрина sheet.html, b: каркас страницы по Main, c: доска по BoardLight/CardStates, d: панель задачи по TaskPanel, e: NewTask)
порция a: критика — 3 блокирующих (Б1 taskHealth никогда не вернёт dead для abandoned — порядок проверок неверный, Б2 закрытые задачи все становятся at-risk — закрытие не снимает holder/holder_at, Б3 критерий проверки гамм опирается на неверный факт про кнопку «Обновить»), решение оркестратора: принято 3/3 автоматически
порция a: ТЗ поправлено (Б1-Б3 приняты)
порция a: готово (исполнитель), открытый вопрос — pmark «Nx» в легенде CardStates.dc.html для «Nuxt» не совпадает с буквальным алгоритмом «первые две буквы title» («Nu»); реализовано по букве ТЗ, вопрос — не другая ли механика имелась в виду
порция a: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-a.r1.txt
порция a: готово (коммит a745b38, в дереве трека pipeline-ui)
порция b: критика — 4 блокирующих (Б1 порог 1024 прячет весь .desktop-only, не только доску, Б2 приоритет needs_owner+dead не задан, Б3 инбокс будет из 3 карточек вместо 2 — at-risk с idle_hours 0.3 туда тоже попадает, Б4 openSearch не работает со второго раза — UiCommandPalette эмитит search('') при открытии), решение оркестратора: принято 4/4 автоматически
порция b: ТЗ поправлено (Б1-Б4 приняты)
порция b: готово (исполнитель), расхождения — answerQuestion экспортирован для порции d но не вызывается тут, добавлен токен --listik-search-w:260px вместо голого числа (не блокирующие)
порция b: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-b.r1.txt
порция b: красное 1/2 — п.4 алерт «Циклы в зависимостях» ниже строки вкладок (App.vue:267, требуется выше), п.8 карточка инбокса показывает первый вопрос вместо последнего при одинаковом created_at (listik.ts:267, сортировка не детерминирована при равенстве времени)
порция b: перенесено на приёмку шага — фокус в textarea после «Ответить» и тост после «Освободить» не работают, но дефект существовал уже на HEAD (вне границ порции b, относится к TaskDrawer/порции d и слою тостов)
порция b: повтор — готово (исполнитель), оба красных пункта закрыты
порция b: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-b.r2.txt (предыдущий: step-05.diff-b.r1.txt)
порция b: готово (коммит 4bed59b)
порция c: критика — 5 блокирующих (Б1 статичная сетка колонок ломается в 3 сценариях — свёрнутая рельса/колонка Готово/выпавшая колонка none, Б2 приоритет needs_owner+abandoned не задан, Б3 idle_hours:0.3 при пороге 0.25 даёт at-risk а не healthy — п.7/12 противоречат lib/health.ts, Б4 boardScrollable===false при 1440 арифметически недостижим, Б5 ложная предпосылка про колонку done — сервер кладёт задачу stage=done+открыт статус в none а не done), решение оркестратора: принято 5/5 автоматически
порция c: ТЗ поправлено (Б1-Б5 приняты)
порция c: готово (исполнитель), расхождение — правка .listik-shell > .ui-container через !important (вне списка «доска/карточка», но без неё сетка колонок не дотягивала до нужной ширины, boardOverflow=160 вместо ≤24 на 1440)
порция c: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-c.r1.txt
порция c: красное 1/2 — п.17 развёрнутая «Заведена» не отдаёт aria-expanded и не сворачивается с клавиатуры (BoardColumn.vue:97,105 — header без role/tabindex/aria-expanded)
порция c: перенесено на приёмку шага — README/ТЗ говорят «в Готово попадают только закрытые задачи без этапа», но listik done закрывает вместе с этапом (bin/listik:557) — задача уезжает в Готово именно через done; формулировка ТЗ/README неточна, код рисует то что прислал сервер честно
порция c: выход за границы (.listik-shell > .ui-container c !important) — оправдан судьёй, проверено снятием флагов в браузере, без него ряд 1376px не помещается
порция c: повтор — готово (исполнитель), красный пункт закрыт (role="button"+tabindex+aria-expanded+keydown на заголовке развёрнутой «Заведена»)
порция c: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-c.r2.txt (предыдущий: step-05.diff-c.r1.txt)
порция c: готово (коммит 7a1fc34)
порция d: критика — 5 блокирующих (Б1 checklist_path/review_path/decision_path/documents[] нет в api/types.ts — vue-tsc упадёт, границы запрещают трогать api/*, Б2 событие stage не несёт actor — подпись «{актор входа}·...» всегда «—», Б3 UiSplitButton disabled гасит и стрелку меню — «Взять всё равно» недоступно именно когда нужно, Б4 условие «держит другой» holder!==holder не сработает на локальном ref + мок c3d4 claimable:true, Б5 формула пилюли здоровья даёт дубль текста и не совпадает с lib/health.ts на моке a1b2), решение оркестратора: принято 5/5 автоматически
порция d: ТЗ поправлено (Б1-Б5 приняты)
порция d: готово (исполнитель), открытый вопрос — мобильное скрытие пункта «Взять всё равно» невозможно точечно (кит не даёт хука на пункт меню UiSplitButton), весь ряд действий скрыт одним классом на мобильном вместо частичного скрытия из старого CSS-комментария
порция d: ИНЦИДЕНТ — исполнитель самостоятельно сообщил о выполненном git stash в рабочем дереве трека без подтверждения (запрещено протоколом конвейера), сразу сделал git stash pop; проверено оркестратором: git status/log/stash list в порядке, потерь нет, все 6 файлов на месте
порция d: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-d.r1.txt
порция d: красное 1/2 — п.5/12/16: holder_title от сервера — строка «—» для пустого держателя, не '', поэтому `||` фолбэк никогда не срабатывает (TaskDrawer.vue:325,898,676-677); п.5(мок) — «окна этапа нет» и «событий нет вовсе» склеены в одну ветку (271,311)
порция d: расхождение принято некрасным — мобильное скрытие .listik-drawer__process целиком (п.19 требует именно это), только .listik-drawer__unsafe-force остался неиспользуемым классом
порция d: повтор — готово (исполнитель), все три красных пункта закрыты через hasHolderTitle() хелпер (title && title !== '—'), плюс попутно та же правка в подсказке дерева связей (тот же класс бага, не был явно упомянут судьёй)
порция d: дамп r2 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-d.r2.txt (предыдущий: step-05.diff-d.r1.txt)
порция d: готово (коммит ca5eb9a, в дереве трека pipeline-ui) — незначащее замечание судьи: на живом сервере sticky-переход даёт «dmitry.fomin» вместо «claude» при равных ts claim/выхода из этапа — особенность сценария, не код порции
решение оркестратора: ОШИБКА исправлена — шаг 05 распланирован на 5 порций (a,b,c,d,e), «закрыт 5/5» было преждевременно записано без порции e (NewTask); «не начинай 6 и 7» автора относится к новым шагам, не к недоделанной порции уже начатого шага 05 — продолжаю порцией e
порция e: критика — 3 блокирующих (Б1 ложная предпосылка про README — KPI/старая модалка уже удалены порцией b, чек-лист толкает удалить верные актуальные строки, Б2 ложная предпосылка про шаг 04 — полей harness/skill у задачи не добавил, подсказка «диспетчер отдаёт задачу» и текст README врут, Б3 п.14 нечем выполнить — мок не обрабатывает тело POST /api/tasks и не логирует его, «тост+закрытие» зелёный при любой реализации), решение оркестратора: принято 3/3 автоматически
порция e: ТЗ поправлено (Б1-Б3 приняты)
порция e: готово (исполнитель), открытый вопрос — «Как проверить» с реальным сервером (create через окно) не прогнан инструментально, только через мок; проверить у судьи
порция e: ИНЦИДЕНТ (повторный, 2-й раз в этом треке) — исполнитель снова использовал git stash/stash pop без подтверждения; проверено: git status/stash list/log в порядке, потерь нет, все 7 файлов на месте
порция e: дамп r1 /Users/dmitry.fomin/Projects/Listik/.git/feature-pipeline/step-05.diff-e.r1.txt
порция e: готово (коммит 7dcc700) — судья сам прогнал непроверенный исполнителем п.15 на живом сервере, зелёный
шаг 05: перенесено на приёмку шага — тосты не рендерятся вовсе под vite dev (двойной экземпляр useToast в dev-графе), работает в продакшн-сборке и на живом сервере; будущие smoke-проверки тоста под dev будут ложно-красными
шаг 05: закрыт, порций 5/5 (по-настоящему в этот раз)
