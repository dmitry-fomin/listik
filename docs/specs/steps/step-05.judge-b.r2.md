# Приёмка порции 05.b, заход r2 — зелёный

Дерево трека: `/Users/dmitry.fomin/Projects/Listik-ui` (ветка `pipeline-ui`, HEAD до коммита `a745b38`).
Заход повторный: проверялась **только разница** между `step-05.diff-b.r1.txt` и `step-05.diff-b.r2.txt`.

## Что изменилось между заходами

`diff -u` двух пакетов даёт ровно две содержательные правки (остальное — сдвиг номеров строк в
заголовках ханков и хеши блобов):

1. `web/src/App.vue` — `UiAlert` с циклами перенесён из позиции после строки вкладок в позицию
   **до** неё (третьим алертом в `.listik-shell__top`, сразу за `connectionLost`/`lastError`);
   класс `desktop-only` с него снят в r1 и не вернулся.
2. `web/src/store/listik.ts:265–273` — выбор последнего вопроса переписан с
   `sort(...)[0]` на `reduce`: при равном `created_at` берётся кандидат, стоящий **позже**
   в ответе API.

Других изменённых файлов в разнице нет; состав диффа (16 путей) прежний.

## Красные пункты r1

### 1. п.4 — алерт «Циклы в зависимостях» ниже строки вкладок — **закрыт**

Проверка на моке с подставленными циклами (копия `scripts/mock-api.mjs` в scratchpad,
`cycles: [['listik-web-a1b2','listik-api-c3d4','listik-web-a1b2']]`, порт 8788; `npx vite --port 5199`),
headless Chrome через CDP:

```
cycleAlertPresent: true, tabsFollowsAlert: true
cycleTop: 88, tabsTop: 181, inboxTop: 286, toolbarTop: 486
```

`alert.compareDocumentPosition(tabs)` даёт `DOCUMENT_POSITION_FOLLOWING` — вкладки идут после
алерта и по DOM, и геометрически. `.listik-shell__top` — `display:flex; flex-direction:column`
без правил `order` (`app.css:96–100`), поэтому порядок DOM = порядок на экране.
Алерт «Сервер Listik недоступен» стоит ещё выше (в шаблоне первым), его код не менялся.

### 2. п.8 — в карточке инбокса показывался первый вопрос, а не последний — **закрыт**

Живой сервер на временной базе (`LISTIK_DB`/`LISTIK_CONFIG` во временном каталоге, порт 8792,
остановлен после проверки), сценарий п.8 дословно:
`new` → `claim listik-c086 --actor agent:claude` → `needs-owner … "Первый"` → `needs-owner … "Второй"`.
Оба комментария записаны в одну секунду (условие, на котором ломался r1):

```
('listik-c086:1789233425742:407', 'question', 'Первый', '2026-09-12T17:17:05Z')
('listik-c086:1789233425824:648', 'question', 'Второй', '2026-09-12T17:17:05Z')
```

`GET /api/tasks/listik-c086` отдаёт их в порядке вставки (`store.py:503` — `ORDER BY created_at`,
при равенстве сохраняется порядок вставки): `Первый`, `Второй`. Страница (`vite` на 5200,
`VITE_API_BASE` на временный сервер), CDP:

```
inboxQuestionLine: "Claude спрашивает: Второй"
```

Регрессия на соседнем случае проверена там же: третий вопрос в другую секунду
(`needs-owner listik-c086 "Третий вопрос судьи"`) вытесняет оба — карточка показывает
`Claude спрашивает: Третий вопрос судьи` (заодно подтверждает, что ветка «вопрос автору»
у взятой в работу задачи сохраняется, вторая половина п.8).

## Проверка, что фикс ничего не сломал в пределах разницы

- `npm run typecheck` и `npm run build` — зелёные (`vue-tsc --noEmit` без вывода; сборка `✓ built`).
  `reduce` с `(typeof questions)[number] | undefined` типизируется.
- На моке при 1440: вкладки `["Доска (5)","Список","Таймлайн (0)","Метрики"]`, `.listik-inbox-card` = 3,
  `.listik-toolbar .ui-chip` = 6 — перенос алерта не сдвинул каркас. В консоли только 404 favicon
  (был и в r1, и на HEAD).
- Ширина 900 (видимость по `getBoundingClientRect().height`): алерт циклов 69px, вкладки 36px,
  инбокс 443px, тулбар 100px — видимы; `.listik-column` = 0, `.listik-mobile-queue` = 220px.
  То есть п.17 («при 900 видны вкладки, инбокс, тулбар и алерт циклов, если он есть») держится
  и после переноса алерта.
- Секретов в диффе нет: `.env`/`*.key`/`*.pem`/`credentials.json` отсутствуют, значений токенов
  и содержимого `config.toml` нет. Временный токен проверки жил только во временном конфиге
  в scratchpad и в дерево трека не попадал.

## Перенесено на приёмку шага (из r1, без изменений)

- **п.7, фокус в `textarea` после «Ответить»** — гонка `drawerRef.focusComment()` с
  `useDialogA11y.activate()` кита; воспроизводится и на HEAD. Территория `TaskDrawer` (порция d).
- **п.7, тост после «Освободить»** — тосты не показываются нигде в приложении, включая HEAD;
  `POST /api/tasks/<id>/release` уходит и доска обновляется. Слой тостов вне границ правки порции.

Полная проверка чек-листа заново не гонялась (заход повторный): зелёные пункты r1 №№1–3, 5–7, 9–16,
18–21 приняты как есть, код под ними в разнице заходов не менялся.

## Артефакты

Временные (копия мока с циклами, CDP-сценарии, база и конфиг живого сервера) — в scratchpad сессии;
сервер остановлен (`./bin/listik stop`), dev-серверы и Chrome сняты. Рабочее дерево трека до коммита
показывало те же 16 путей порции.
