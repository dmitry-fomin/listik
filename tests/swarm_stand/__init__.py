"""Стенд роя Listik.

Это модель проекта роя для стенда, а не реализация: планировщик, диспетчер
и барьер здесь не живут — части этого пакета подменяются настоящими модулями
в последующих swarm-порциях. Зависимости пакета — только stdlib и `git` в
PATH; ничего из `listik/` сюда не импортируется.
"""
